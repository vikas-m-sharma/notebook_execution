# Development and test-only dependencies (CQ-005)
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Testing framework
pytest>=7.0.0,<9.0.0
pytest-asyncio>=0.21.0,<1.0.0

# HTTP test client
httpx>=0.24.0,<1.0.0

# SQLite async driver for test database
aiosqlite>=0.19.0,<1.0.0
